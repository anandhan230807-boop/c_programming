#include<stdio.h>
int main(){
	int a,surfacearea,volume,perimeter;
	scanf("%d",&a);
	surfacearea=6*a*a;
	volume=a*a*a;
	perimeter=12*a;
	printf("%d\n%d\n%d",surfacearea,volume,perimeter);
	return 0;
}







 //surface area 6*a*a
 // volume a*a*a
 // perimeter 12*a